{% extends 'base.html.twig' %}

{% block title %}Products{% endblock %}

{% block body %}
    <div class="container">
    </div>
    <h1>Liste des produits</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Nom du produits</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
            <tr>
                <th>Prix unitaire</th>
                <td>{{ product.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_products_index') }}">Retour à la liste de produit</a>

    <a href="{{ path('app_products_edit', {'id': product.id}) }}">Modifier un produit</a>

    {{ include('products/_delete_form.html.twig') }}
{% endblock %}
